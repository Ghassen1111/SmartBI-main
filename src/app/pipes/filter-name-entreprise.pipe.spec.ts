import { FilterNameEntreprisePipe } from './filter-name-entreprise.pipe';

describe('FilterNameEntreprisePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterNameEntreprisePipe();
    expect(pipe).toBeTruthy();
  });
});
